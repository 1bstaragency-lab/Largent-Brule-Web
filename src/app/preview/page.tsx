import { HomepageLive } from "@/components/homepage-live";

// "Browse Anyway" sneak-peek destination, linked from the still-locked
// countdown page. Shows the lookbook world (collage) only — no
// products, no checkout surface — collections stays locked separately.
export default function PreviewPage() {
  return <HomepageLive minimal />;
}
